build site:
  image: node:6
  stage: build
  script:
    - npm install --progress=false
    - npm run build
  artifacts:
    expire_in: 1 week
    paths:
      - dist

deploy:
  image: node:6
  stage: deploy
  script:
    - aws s3 sync ./dist s3://www.wilks2020calculator.com